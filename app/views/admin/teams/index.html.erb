<section class='container px-4 mx-auto'>
  <% breadcrumbs = [
       { title: t('views.admin.teams'), path: admin_teams_path }
     ] %>
  <%= render partial: 'shared/admin/breadcrumbs', locals: { breadcrumbs: } %>

  <div class='mt-6'>
    <%= render 'teams' %>

    <%= render partial: 'shared/admin/tables/pagination', locals: { items: @teams } %>
  </div>
</section>
